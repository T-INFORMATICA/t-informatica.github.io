---
layout: null
---

{
    {%- for coursedata in site.data.courses %}
        {% assign rubricFound = false %}
        {% for subjectdata in coursedata.subjects %}
            {% for rubric in subjectdata.rubrics %}
            {% assign rubricFound = true %}
    "{{subjectdata.name}}": {
        "A": "{{rubric.A}}",
        "B": "{{rubric.B}}",
        "C": "{{rubric.C}}",
        "D": "{{rubric.D}}",
        "E": "{{rubric.E}}"
    }
                {%- unless forloop.last %},{% endunless -%}
            {% endfor %}
            {% if rubricFound == true %},{% endif %}
        {% endfor %}
        {% if rubricFound == true %},{% endif %}
    {% endfor %}
}